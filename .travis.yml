language: bash

before_script:
    - sudo apt-get update
    - sudo apt-get install -y make debootstrap
    - mkdir build archive

script:
    - export ROLES="base openstack-common openstack-full"
    - sudo make TOP=build ARCHIVE=archive DIST=wheezy DVER=D7 openstack-full

notifications:
  emails: false
